# AI 模型 API 连接调试复盘报告

> 项目：flow-review-app（流程图规范评审系统）
> 厂商：通义千问 Token Plan 个人版
> 模型：qwen3.8-max-preview
> 复盘日期：2026-07-31

---

## 数据概览

本次调试过程发生在 2026-07-31 上午会话中，共经历 **3 轮排查 + 4 次代码修复**，最终将解析成功率从 0% 提升至中小图 100%。

| 指标 | 数值 |
|------|------|
| 涉及工具调用 | Read / Write / Execute / Replace |
| 修改文件数 | 6 个（.env, runtime_settings.json, llm_client.py, image_llm_parser.py, database.py, tasks.py, main.py） |
| 诊断手段 | 日志排查、DNS/TCP/API 分层测试、Python 直接调用验证 |
| 最终耗时 | 小图 ~146s（Step1 63s + Step2 83s），大图仍超时 |

---

## 一、踩过的坑

### 坑 1：Base URL 域名拼写错误导致 DNS 解析失败 ★

**现象**：
```
图片解析失败，无法进行流程图规范评审。原因：LLM 调用失败: Connection error.
```

**日志定位**：
```
openai.APIConnectionError: [Errno 8] nodename nor servname provided, or not known
```

**根因**：`.env` 文件中 Base URL 写的是：
```
https://token-plan.cn-beijing.maas.aliyun.com/compatible-mode/v1
```
正确域名应为 `aliyuncs.com`（阿里云服务 API 域名），少了一个 `c`。

**为什么之前用 Python 脚本直接测试成功**：因为直接测试时用了正确的 `aliyuncs.com` 域名，但 `.env` 和 `runtime_settings.json` 里写的是错的。

**教训**：
- 配置项需要一次性校验 URL 可达性，而不是等运行时才发现
- 多个配置文件（`.env` + `runtime_settings.json`）同时维护同一值，存在不一致风险。应使用单一配置源

**修复**：
- `.env`: `LLM_BASE_URL` 域名 `aliyun.com` → `aliyuncs.com`
- `backend/data/runtime_settings.json`: 同步修正

---

### 坑 2：图片 + 长 System Prompt + 大 max_tokens 导致服务端超时 ★★★

**现象**：
```
图片解析失败，无法进行流程图规范评审。原因：LLM 调用失败: Request timed out.
```

**诊断过程**：

| 测试场景 | 耗时 | 结果 |
|----------|------|------|
| 纯文本（"回复OK"） | 3.5s | ✅ |
| 视觉（339B 白图 + 简单 prompt） | 18.2s | ✅ |
| 视觉（实际流程图 + 简单 prompt） | 4.9s | ✅ |
| **视觉（流程图 + 完整 system prompt + max_tokens=4000）** | **180s** | **❌ 超时** |

**根因**：Token Plan 个人版 `qwen3.8-max-preview` 在单次请求中同时处理图片识别 + 长 system prompt（~800 字要求输出复杂 JSON）+ 大 max_tokens（4000），服务端处理时间累计超过 180 秒读超时。

**本质**：Token Plan 个人版面向 IDE 编程助手场景，不是面向高吞吐 API 调用的企业级产品，视觉推理队列优先级和并发受限。

**最初误判**：一开始认为是图片体积大（218KB），加了 `_compress_image` 压缩到 128KB 后仍然超时 — 证明根因不是图片大小。

**修复方案**：两步拆解策略
- **Step 1**：图片 → 文字描述（简单 prompt，max_tokens=2000）→ ~63s
- **Step 2**：文字描述 → 结构化 JSON（纯文本，无图片）→ ~83s
- 总计 ~146s，虽然慢但能成功完成

**教训**：
- 调试时应该先**隔离变量**，逐一排除（DNS→纯文本→小图+简单 prompt→完整 prompt），而不是凭猜测改代码
- "慢"比"不返回"更难排查，因为问题在远程服务器端

---

### 坑 3：大图（>1MB）即使两步拆解也超时 ★★

**现象**：
1082.6KB 的流程图图片，压缩后 96KB 发到 Step 1（简单描述），仍然 **180 秒超时**。

**根因**：图片 Step 1 使用了 `_compress_image` 将 1082KB 压到 96KB（质量 75，尺寸 2048×1455），压缩后体积不大，但**图片原始像素量多**（2048×1455），视觉模型仍需处理大量像素 —— Token Plan 个人版对于高分辨率图片的视觉编码阶段本身就很慢。

**教训**：
- 图片体积 ≠ 模型处理时间，**像素分辨率**才是决定视觉模型推理时间的关键因素
- 云端视觉模型的推理时间包含**图片编码**和**文本生成**两个阶段，压缩只减小传输体积，不减少编码计算量

---

### 坑 4：SQLite CURRENT_TIMESTAMP 默认 UTC 时间，创建时间偏 8 小时 ★

**现象**：任务列表里创建时间比实际时间早 8 小时（如：北京时间 11:07 创建的任务，列表中显示 03:07）。

**根因**：SQLite 的 `CURRENT_TIMESTAMP` 默认返回 **UTC 时间**，而应用中 UTC+8 使用的是北京时区，全程没有做时区转换。

**修复**：3 处改动
1. `database.py`：`DEFAULT CURRENT_TIMESTAMP` → `DEFAULT (datetime('now', 'localtime'))`
2. `tasks.py`：INSERT 时显式传入 `datetime('now', 'localtime')`
3. `main.py`：卡住任务判断 `datetime('now', '-10 minutes')` → `datetime('now', 'localtime', '-10 minutes')`

**教训**：
- 任何涉及时间字段的数据库设计，应该从一开始就明确**存储什么时区**、**何时转换**的策略
- `CURRENT_TIMESTAMP` 的行为在不同数据库（MySQL vs SQLite vs PostgreSQL）中不同，不能想当然

---

## 二、关键决策提炼

### 决策 1：两步拆解 vs 一次性调用

| 维度 | 一次性调用 | 两步拆解 |
|------|-----------|----------|
| 实现复杂度 | 简单 | 中等（需管理两步状态） |
| 成功率 | 0%（超时） | 中小图 100% |
| 总耗时 | N/A（永不完成） | ~146s |
| 灵活度 | 低 | 高（可单独优化每步） |

**选择**：两步拆解

**决策依据**：通过隔离变量实验（见坑 2），确定根因是"视觉 + 复杂输出"合并导致服务端超时，而非 API 不通或图片大小。拆解后每步复杂度可控，总耗时虽长但可完成。

**反思**：这是正确的工程决策。更优的方案是引入**混合模型策略**——视觉用专业 VL 模型，文本结构化用便宜快速的文本模型。

---

### 决策 2：图片压缩策略

**选择**：`Pillow` 库 + 渐进质量压缩（75→60→45→30），目标 ≤150KB

**优点**：
- 减少网络传输时间
- 对简单 prompt 场景不影响识别准确率

**局限**：
- 只压缩体积，不减少像素数（对模型推理时间影响有限，见坑 3）
- 应该额外限制最大分辨率（如 1024×768），而不是仅 2048px

---

## 三、调试方法论亮点

### 亮点 1：分层隔离诊断法

整个调试过程体现了有效的**分层隔离**策略：

```
第 1 层 — DNS 解析：socket.getaddrinfo() 测试域名可达性
第 2 层 — TCP 连接：socket.connect_ex() 测试端口可达
第 3 层 — 纯文本 API：OpenAI SDK 纯文本请求测试鉴权和连通
第 4 层 — 视觉小图：339B 白图测试视觉管线基本可用性
第 5 层 — 视觉+复杂输出：完整 prompt 测试业务场景
```

**价值**：避免了"到处乱改代码"的无效排查，每一层出问题都能精确定位。

### 亮点 2：用最小可复现测试验证假设

没有直接修改生产代码，而是先用独立 Python 脚本验证每个假设：
- "域名换了能通吗？" → 小图测试 4.9s 成功
- "是图片太大吗？" → 压缩后仍超时，排除
- "是长 prompt 的问题吗？" → 完整 prompt 测试 → 180s 超时，确认

**价值**：快速缩小问题范围，避免在生产代码上反复试错。

---

## 四、高手差距分析

### 4.1 问题描述质量

**现状**：用户反馈"图片解析失败，无法进行流程图规范评审。原因：LLM 调用失败: Connection error." — 直接粘贴了前端报错信息，这对排查有帮助。

**可改进**：可以在提问时附加操作环境和步骤描述，例如"我上传了一张 218KB 的 PNG 流程图，页面一直转圈然后报错"，让排查者无需先从日志了解上下文。

### 4.2 配置管理

**现状**：`LLM_BASE_URL` 同时在 `.env` 和 `runtime_settings.json` 中维护，存在不一致风险。

**高手做法**：单一配置源（仅 `.env`），运行时设置通过内存管理而不是持久化到一个会被手动编辑的 JSON 文件。

### 4.3 超时策略

**现状**：全局 180 秒读超时，没有分层超时设计。

**高手做法**：
- 连接超时（connect timeout）：10s
- 首次字节超时（first byte timeout）：30s  
- 读取超时（read timeout）：根据请求复杂度动态设置

### 4.4 可观测性

**现状**：日志级别为 INFO，记录了每次 API 调用的参数和结果。

**可改进**：
- 请求耗时分布统计（P50/P95/P99）
- Token Plan 服务端限流/排队状态感知
- 前端应展示"正在解析图片…"等中间状态，而不是空转圈

---

## 五、后续优化建议

| 优先级 | 建议 | 预期效果 |
|--------|------|----------|
| P0 | Step 1 限制图片分辨率 ≤1024px（不只是压缩体积） | 视觉编码时间大幅降低 |
| P1 | 引入请求超时分段（connect=10s, first_byte=30s, read=300s） | 更快检测网络问题，给复杂请求更多时间 |
| P2 | 前端增加中间状态提示（"正在解析图片…" "正在生成报告…"） | 改善用户等待体验 |
| P2 | 统一配置源，去掉 runtime_settings.json 中的 LLM_BASE_URL | 避免配置不一致 |
| P3 | 增加 API 调用耗时监控（请求数、P50/P95/P99、成功率） | 提前发现性能劣化 |

---

## 六、调试时间线

```
10:30 — 用户首次上传流程图，报错 Connection error
10:32 — 排查日志，发现 DNS 解析失败：域名写的是 aliyun.com
10:33 — 修复 .env 和 runtime_settings.json 域名 → aliyuncs.com，重启后端
10:35 — 用户再次上传，仍报错 Request timed out (180s)
10:36 — 第一次误判：认为是图片体积大，加 _compress_image
10:38 — 用户反馈：218KB 图片不算大，换更小图也没用
10:40 — 独立 Python 脚本分层测试：DNS ✅ TCP ✅ 纯文本 ✅ 视觉小图 ✅
10:42 — 完整 prompt 测试 → 180s 超时 ❌ 确认根因
10:43 — 实施两步拆解策略，重启后端
10:59 — 上传大图(1082KB) → 仍超时（大像素问题）
11:07 — 上传小图(218KB) → Step1 63s ✅ Step2 83s ✅ 评审通过 85分
11:08 — 发现创建时间不准，排查 SQLite CURRENT_TIMESTAMP UTC 问题
11:10 — 修复时区 + 修正历史数据
```

---

> 📊 **总结**：从"完全不 work"到"小图能 work"，核心在于放弃"一步到位"的思路，将复杂请求拆解为简单子任务。这是云端 AI API 调试的通用策略——当模型能力不足以在单次调用中完成复杂任务时，拆解往往是比等官方升级更务实的方案。
